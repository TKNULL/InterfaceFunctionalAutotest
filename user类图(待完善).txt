EUSD：
1、每次转账、购买的金额，都在.properties里面设置
2、测试过程中，调用get_EUSD_buy_quantity（）方法，获得每次购买/出售的数量后，把数量存到context里面
3、承兑商和用户的支付密码都是123456
4、承兑商、用户都需要设置收付款方式
#####

~返回BigDecimal:1004800000

get_EUSD_frozen()
get_EUSD_available()






~返回BigInteger：
get_payment_methods_return_pay_id（）获取pay_id
execute_buy_return_id（）执行购买操作并返回订单ID




















